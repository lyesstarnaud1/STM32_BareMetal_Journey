/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>

// --- THE MEMORY MAP (Addresses from the Reference Manual) ---
#define RCC_BASE      0x40023800UL
#define RCC_AHB1ENR   (RCC_BASE + 0x30UL) // Clock Enable Register

#define GPIOA_BASE    0x40020000UL
#define GPIOA_MODER   (GPIOA_BASE + 0x00UL) // Mode Register
#define GPIOA_ODR     (GPIOA_BASE + 0x14UL) // Output Data Register

int main(void) {
    // 1. ENABLE CLOCK FOR GPIOA
    // We need to set Bit 0 of the RCC_AHB1ENR register.
    // We cast the address to a pointer of 'volatile uint32_t'
    volatile uint32_t *p_clk_en = (volatile uint32_t *) RCC_AHB1ENR;
    *p_clk_en |= (1 << 0); // Math: Set bit 0

    // 2. CONFIGURE PA5 AS OUTPUT
    // In MODER register, each pin takes 2 bits. PA5 is bits 10 and 11.
    // Output mode is '01'.
    volatile uint32_t *p_gpioa_mode = (volatile uint32_t *) GPIOA_MODER;
    *p_gpioa_mode &= ~(3 << 10); // Clear bits 10 and 11
    *p_gpioa_mode |= (1 << 10);  // Set bit 10 to 1 (01 = Output)

    // 3. THE LOOP
    volatile uint32_t *p_gpioa_odr = (volatile uint32_t *) GPIOA_ODR;

    while(1) {
        // Toggle Pin PA5
        *p_gpioa_odr |= (1 << 5);

        // A simple math-based delay loop
        for(uint32_t i = 0; i < 1000; i++);

        *p_gpioa_odr &= ~(1 << 5);

                // A simple math-based delay loop
                for(uint32_t i = 0; i < 1600000; i++);
    }
}
